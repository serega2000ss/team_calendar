<dl>
  <% events.each do |event| %>
    <dt><%= event.due_date.to_formatted_s(:long) %></dt>
    <dd>
      <%= link_to "#{event.event_type.title}: #{event.name}", event_path(event) %>
      <p><%= event.description %></p>
    </dd>
  <% end %>
</dl>